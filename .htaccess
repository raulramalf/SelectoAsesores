<IfModule mod_rewrite.c>
    RewriteEngine On
    # Si la petición no empieza por /public, la mandamos dentro
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>